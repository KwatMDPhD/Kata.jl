module TEMPLATE

const DP = dirname(@__DIR__)

const DI = joinpath(DP, "input")

const DU = joinpath(DP, "output")

# ----------------------------------------------------------------------------------------------- #

end
